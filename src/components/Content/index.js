import { memo } from "react";

const Content = () => {
    return (
        <main>
            <div>
                <h1>Content</h1>
            </div>
        </main>
    )
};

export default memo(Content);
