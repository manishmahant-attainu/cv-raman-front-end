import { memo } from "react";

const Footer = () => {
    return (
        <footer>
            <div>
                <h1>Footer</h1>
            </div>
        </footer>
    )
};

export default memo(Footer);
